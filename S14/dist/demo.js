"use strict";
// function getRandomElement <T> (list: T[]): T {
//     const ranIdx = Math.floor(Math.random() * list.length)
//     return list[ranIdx]
// }
const getRandomElementt = (list) => {
    const ranIdx = Math.floor(Math.random() * list.length);
    return list[ranIdx];
};
